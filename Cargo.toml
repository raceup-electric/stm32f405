[package]
name = "virtual_embassy_test"
version = "0.1.0"
edition = "2021"

[dependencies]
embassy-sync = "0.6.1"
embassy-executor = { version = "^0.6.3", features = ["arch-std", "executor-thread"] }
embassy-time = "0.3.2"
embassy-usb = "0.3.0"
# embassy-net = { version = "^0.5.0", features=[ "std",  "log", "medium-ethernet", "medium-ip", "tcp", "udp", "dns", "dhcpv4", "proto-ipv6"]}
# embassy-net-wiznet = "0.1.0"
embassy-futures = "0.1.1"

# defmt = "^0.3"
# defmt-rtt = "^0.4"

static_cell = "2.1.0"

[profile.release]
debug = 2
